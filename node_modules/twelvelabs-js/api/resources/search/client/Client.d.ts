/**
 * This file was auto-generated by Fern from our API Definition.
 */
import * as environments from "../../../../environments";
import * as core from "../../../../core";
import * as TwelvelabsApi from "../../../index";
export declare namespace Search {
    interface Options {
        environment?: core.Supplier<environments.TwelvelabsApiEnvironment | string>;
        /** Specify a custom URL to connect the client to. */
        baseUrl?: core.Supplier<string>;
        apiKey: core.Supplier<string>;
    }
    interface RequestOptions {
        /** The maximum time to wait for a response in seconds. */
        timeoutInSeconds?: number;
        /** The number of times to retry the request. Defaults to 2. */
        maxRetries?: number;
        /** A hook to abort the request. */
        abortSignal?: AbortSignal;
        /** Additional headers to include in the request. */
        headers?: Record<string, string>;
    }
}
export declare class Search {
    protected readonly _options: Search.Options;
    constructor(_options: Search.Options);
    /**
     * Use this endpoint to search for relevant matches in an index using text, media, or a combination of both as your query.
     *
     * **Text queries**:
     * - Use the `query_text` parameter to specify your query.
     *
     * **Media queries**:
     * - Set the `query_media_type` parameter to the corresponding media type (example: `image`).
     * - Provide up to 10 images by specifying the following parameters multiple times:
     *   - `query_media_url`: Publicly accessible URL of your media file.
     *   - `query_media_file`: Local media file.
     * **Composed text and media queries**:
     * - Use the `query_text` parameter for your text query.
     * - Set `query_media_type` to `image`.
     * - Provide up to 10 images by specifying the `query_media_url` and `query_media_file` parameters multiple times.
     *
     * **Entity search** (beta):
     * - To find a specific person in your videos, enclose the unique identifier of the entity you want to find in the `query_text` parameter.
     *
     * <Note title="Notes">
     * - When using images in your search queries (either as media queries or in composed searches), ensure your image files meet the [requirements](/v1.3/docs/concepts/models/marengo#image-file-requirements).
     * - This endpoint is rate-limited. For details, see the [Rate limits](/v1.3/docs/get-started/rate-limits) page.
     * </Note>
     *
     * @param {TwelvelabsApi.SearchCreateRequest} request
     * @param {Search.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link TwelvelabsApi.BadRequestError}
     * @throws {@link TwelvelabsApi.TooManyRequestsError}
     *
     * @example
     *     await client.search.create({
     *         indexId: "index_id",
     *         searchOptions: ["visual"]
     *     })
     */
    create(request: TwelvelabsApi.SearchCreateRequest, requestOptions?: Search.RequestOptions): core.HttpResponsePromise<TwelvelabsApi.SearchResults>;
    private __create;
    /**
     * Use this endpoint to retrieve a specific page of search results.
     *
     * <Note title="Note">
     * When you use pagination, you will not be charged for retrieving subsequent pages of results.
     * </Note>
     *
     * @param {string} pageToken - A token that identifies the page to retrieve.
     * @param {TwelvelabsApi.SearchRetrieveRequest} request
     * @param {Search.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link TwelvelabsApi.BadRequestError}
     *
     * @example
     *     await client.search.retrieve("1234567890", {
     *         includeUserMetadata: true
     *     })
     */
    retrieve(pageToken: string, request?: TwelvelabsApi.SearchRetrieveRequest, requestOptions?: Search.RequestOptions): core.HttpResponsePromise<TwelvelabsApi.SearchRetrieveResponse>;
    private __retrieve;
    protected _getCustomAuthorizationHeaders(): Promise<{
        "x-api-key": string;
    }>;
}
