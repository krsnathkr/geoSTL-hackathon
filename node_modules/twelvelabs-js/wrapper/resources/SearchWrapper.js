"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.SearchWrapper = void 0;
const Client_1 = require("../../api/resources/search/client/Client");
const TwelvelabsApi = __importStar(require("../../api/index"));
const core = __importStar(require("../../core"));
const environments = __importStar(require("../../environments"));
const errors = __importStar(require("../../errors/index"));
const serializers = __importStar(require("../../serialization/index"));
class SearchWrapper extends Client_1.Search {
    constructor(options) {
        super(options);
    }
    /**
     * Use this endpoint to search for relevant matches in an index using text, media, or a combination of both as your query.
     *
     * **Text queries**:
     * - Use the `query_text` parameter to specify your query.
     *
     * **Media queries**:
     * - Set the `query_media_type` parameter to the corresponding media type (example: `image`).
     * - Specify either one of the following parameters:
     *   - `query_media_url`: Publicly accessible URL of your media file.
     *   - `query_media_file`: Local media file.
     *   If both `query_media_url` and `query_media_file` are specified in the same request, `query_media_url` takes precedence.
     *
     * **Multiple images** (Marengo 3.0 only):
     * - Use `query_media_urls` (array of URLs) or `query_media_files` (array of local files) to pass up to 10 images.
     *
     * **Composed text and media queries** (Marengo 3.0 only):
     * - Use the `query_text` parameter for your text query.
     * - Set `query_media_type` to `image`.
     * - Specify the image using either the `query_media_url` or the `query_media_file` parameter.
     *
     *   Example: Provide an image of a car and include  "red color"  in your query to find red instances of that car model.
     *
     * <Note title="Note">
     *   When using images in your search queries (either as media queries or in composed searches), ensure your image files meet the [format requirements](/v1.3/docs/concepts/models/marengo#image-file-requirements).
     * </Note>
     *
     * <Note title="Note">
     * This endpoint is rate-limited. For details, see the [Rate limits](/v1.3/docs/get-started/rate-limits) page.
     * </Note>
     *
     * @param {SearchWrapper.QueryRequest} request
     * @param {Search.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link TwelvelabsApi.BadRequestError}
     * @throws {@link TwelvelabsApi.TooManyRequestsError}
     *
     * @example
     *     await client.search.query({
     *         indexId: "index_id",
     *         searchOptions: ["visual"]
     *     })
     */
    query(request, requestOptions) {
        return __awaiter(this, void 0, void 0, function* () {
            const { queryMediaFile, queryMediaFiles, queryMediaUrls } = request, baseRequest = __rest(request, ["queryMediaFile", "queryMediaFiles", "queryMediaUrls"]);
            const hasPlural = queryMediaFiles != null || queryMediaUrls != null || Array.isArray(queryMediaFile);
            let response;
            if (hasPlural) {
                response = yield this._queryWithFiles(request, requestOptions);
            }
            else {
                const singleRequest = Object.assign(Object.assign({}, baseRequest), (queryMediaFile != null ? { queryMediaFile: queryMediaFile } : {}));
                response = yield this.create(singleRequest, requestOptions);
            }
            const retrieveRequest = {};
            if (request.includeUserMetadata) {
                retrieveRequest.includeUserMetadata = request.includeUserMetadata;
            }
            return new core.Pageable({
                response,
                rawResponse: {},
                hasNextPage: (response) => { var _a; return ((_a = response === null || response === void 0 ? void 0 : response.pageInfo) === null || _a === void 0 ? void 0 : _a.nextPageToken) != null; },
                getItems: (response) => { var _a; return (_a = response === null || response === void 0 ? void 0 : response.data) !== null && _a !== void 0 ? _a : []; },
                loadPage: (response) => {
                    return this.retrieve(response.pageInfo.nextPageToken, retrieveRequest, requestOptions);
                },
            });
        });
    }
    _queryWithFiles(request, requestOptions) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a, _b;
            const { queryMediaFile, queryMediaFiles, queryMediaUrls } = request, rest = __rest(request, ["queryMediaFile", "queryMediaFiles", "queryMediaUrls"]);
            const _request = yield core.newFormData();
            if (rest.queryMediaType != null) {
                _request.append("query_media_type", serializers.SearchCreateRequestQueryMediaType.jsonOrThrow(rest.queryMediaType, {
                    unrecognizedObjectKeys: "strip",
                }));
            }
            // URL fields — plural takes precedence
            if (queryMediaUrls != null) {
                for (const url of queryMediaUrls) {
                    _request.append("query_media_url", url);
                }
            }
            else if (rest.queryMediaUrl != null) {
                _request.append("query_media_url", rest.queryMediaUrl);
            }
            // File fields — plural takes precedence
            const filesToAppend = queryMediaFiles != null
                ? queryMediaFiles
                : Array.isArray(queryMediaFile)
                    ? queryMediaFile
                    : queryMediaFile != null
                        ? [queryMediaFile]
                        : [];
            for (const f of filesToAppend) {
                yield _request.appendFile("query_media_file", f);
            }
            if (rest.queryText != null) {
                _request.append("query_text", rest.queryText);
            }
            _request.append("index_id", rest.indexId);
            for (const _item of rest.searchOptions) {
                _request.append("search_options", serializers.SearchCreateRequestSearchOptionsItem.jsonOrThrow(_item, {
                    unrecognizedObjectKeys: "strip",
                }));
            }
            if (rest.transcriptionOptions != null) {
                for (const _item of rest.transcriptionOptions) {
                    _request.append("transcription_options", serializers.SearchCreateRequestTranscriptionOptionsItem.jsonOrThrow(_item, {
                        unrecognizedObjectKeys: "strip",
                    }));
                }
            }
            // Note: adjustConfidenceLevel is deprecated and ignored by the API. Use the `rank` field in the response instead.
            // Note: threshold is deprecated and ignored by the API. Use the `rank` field in the response instead.
            // Note: sortOption is deprecated and ignored by the API. Use the `rank` field in the response instead.
            if (rest.groupBy != null) {
                _request.append("group_by", serializers.SearchCreateRequestGroupBy.jsonOrThrow(rest.groupBy, {
                    unrecognizedObjectKeys: "strip",
                }));
            }
            if (rest.operator != null) {
                _request.append("operator", serializers.SearchCreateRequestOperator.jsonOrThrow(rest.operator, {
                    unrecognizedObjectKeys: "strip",
                }));
            }
            if (rest.pageLimit != null) {
                _request.append("page_limit", rest.pageLimit.toString());
            }
            if (rest.filter != null) {
                _request.append("filter", rest.filter);
            }
            if (rest.includeUserMetadata != null) {
                _request.append("include_user_metadata", rest.includeUserMetadata.toString());
            }
            const _maybeEncodedRequest = yield _request.getRequest();
            const _baseUrl = (_b = (_a = (yield core.Supplier.get(this._options.baseUrl))) !== null && _a !== void 0 ? _a : (yield core.Supplier.get(this._options.environment))) !== null && _b !== void 0 ? _b : environments.TwelvelabsApiEnvironment.Default;
            const _response = yield core.fetcher({
                url: `${_baseUrl.replace(/\/$/, "")}/search`,
                method: "POST",
                headers: Object.assign(Object.assign(Object.assign({ "X-Fern-Language": "JavaScript", "X-Fern-SDK-Name": "twelvelabs-js", "X-Fern-SDK-Version": "1.2.1", "User-Agent": "twelvelabs-js/1.2.1", "X-Fern-Runtime": core.RUNTIME.type, "X-Fern-Runtime-Version": core.RUNTIME.version }, (yield this._getCustomAuthorizationHeaders())), _maybeEncodedRequest.headers), requestOptions === null || requestOptions === void 0 ? void 0 : requestOptions.headers),
                requestType: "file",
                duplex: _maybeEncodedRequest.duplex,
                body: _maybeEncodedRequest.body,
                timeoutMs: (requestOptions === null || requestOptions === void 0 ? void 0 : requestOptions.timeoutInSeconds) != null ? requestOptions.timeoutInSeconds * 1000 : 60000,
                maxRetries: requestOptions === null || requestOptions === void 0 ? void 0 : requestOptions.maxRetries,
                abortSignal: requestOptions === null || requestOptions === void 0 ? void 0 : requestOptions.abortSignal,
            });
            if (_response.ok) {
                return serializers.SearchResults.parseOrThrow(_response.body, {
                    unrecognizedObjectKeys: "passthrough",
                    allowUnrecognizedUnionMembers: true,
                    allowUnrecognizedEnumValues: true,
                    breadcrumbsPrefix: ["response"],
                });
            }
            if (_response.error.reason === "status-code") {
                switch (_response.error.statusCode) {
                    case 400:
                        throw new TwelvelabsApi.BadRequestError(_response.error.body, _response.rawResponse);
                    case 429:
                        throw new TwelvelabsApi.TooManyRequestsError(_response.error.body, _response.rawResponse);
                    default:
                        throw new errors.TwelvelabsApiError({
                            statusCode: _response.error.statusCode,
                            body: _response.error.body,
                            rawResponse: _response.rawResponse,
                        });
                }
            }
            switch (_response.error.reason) {
                case "non-json":
                    throw new errors.TwelvelabsApiError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.rawBody,
                        rawResponse: _response.rawResponse,
                    });
                case "timeout":
                    throw new errors.TwelvelabsApiTimeoutError("Timeout exceeded when calling POST /search.");
                case "unknown":
                    throw new errors.TwelvelabsApiError({
                        message: _response.error.errorMessage,
                        rawResponse: _response.rawResponse,
                    });
            }
        });
    }
}
exports.SearchWrapper = SearchWrapper;
